// Declaring two numbers
const num1 = 10
const num2 = 2

// Simple Math With Numbers
let val = num1 + num2
val = num1 - num2
val = num1 * num2
val = num1 / num2
val = num1 ** num2
val = num1 % num2

// Math Object
val = Math.PI
val = Math.E
val = Math.round(2.4)
val = Math.ceil(2.4)
val = Math.floor(2.8)
val = Math.sqrt(64)
val = Math.abs(-10)
val = Math.pow(3, 2)

// Returns the minimum number provided
val = Math.min(4, 7 ,3 ,7, 3, -2 , -9)

// Returns the maximum number provided
val = Math.max(6, 7, 9, 3 ,6, 4, -1, 99)

// Returns a random decimal number
val = Math.random()
